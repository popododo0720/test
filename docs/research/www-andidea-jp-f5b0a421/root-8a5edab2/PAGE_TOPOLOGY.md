# Page topology

Root route / is a single sticky video journey, not a vertical collection of screenshots.
Fixed header (z40), centered logo on desktop, left logo/mobile drawer on mobile.
Journey scroll range is 1100svh at desktop; sticky stage occupies one viewport.
Video/poster form independent live layers with holographic pointer transforms and timed ASCII pulses.
Six absolute scenes: Intro/news → Manifesto → Create → Discover → Services → Outro/contact/footer.
Scene markers and playback controls remain available; footer replaces bottom status in scene6.
The controller preserves scroll positions and interactive state, with reduced-motion flow layout.
No other routes existed except the starter root; only that placeholder was replaced.
